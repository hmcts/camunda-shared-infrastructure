#!groovy

@Library('Infrastructure@old-provider') _

def productName = "camunda"
def subscription = "sandbox"
def environment = "sandbox"

withParameterizedInfraPipeline(productName, environment, subscription) {
}
